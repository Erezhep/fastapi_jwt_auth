This is a simple authentication service built with FastAPI. 
Features include:

- User registration and login
- Password hashing with PBKDF2
- JWT-based authentication
- Role-based access control (Doctor, Student, Guest)
- SQLite database for storing users (can be swapped for PostgreSQL)
- Ready for deployment on Google Cloud Run or other container platforms

Perfect for mobile or web apps that need a lightweight auth API.
